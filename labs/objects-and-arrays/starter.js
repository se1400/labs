// Lab 22: Objects & Arrays of Objects
// ─────────────────────────────────────────────────────────
// The programs array is in the HTML tab near the bottom of
// the page. Each item is an OBJECT with five properties:
//   id, name, department, credits, costPerCredit
// Do not modify the array — your JavaScript will read
// (and in Step 5, add to) it.
// ─────────────────────────────────────────────────────────


// ── Step 1: Featured Program (reading object properties) ──


// ── Step 2: renderPrograms (.map() + template literals) ──


// ── Step 3: Control Buttons (.sort() + Clear All) ────────


// ── Step 4: Click a card (event delegation + .find()) ────


// ── Step 5: Add Program form (object literal + .push()) ──

