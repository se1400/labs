// Events & Decisions — JavaScript Lab
// ─────────────────────────────────────────────────────────────────
// The <script defer> in <head> means this file runs after the
// entire page has loaded — no DOMContentLoaded wrapper needed.
//
// Use const for every variable unless you need to reassign it.
// Open the Console tab (bottom panel) to see console.log output.
// Run Tests after each step to check your work.
// ─────────────────────────────────────────────────────────────────


// ── Step 1: addEventListener + click — Dark Mode Toggle ──────────



// ── Step 2: addEventListener + input + setProperty — Font Size ───



// ── Step 3: addEventListener + change + dataset — Department Filter



// ── Step 4: addEventListener + input + if/else — Live Search ─────



// ── Step 5: addEventListener + submit + preventDefault — Inquiry Form
